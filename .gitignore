# PowerShell
*.psm1.bak
*.ps1.bak

# Internal-process notes (kept off the public repo per separation-of-concerns)
.internal/
.claude/

# Archived Bicep source (gitignored — ARM is single source of truth)
.internal/bicep-reference/
.internal/bicep-build.json

# Azure Functions
local.settings.json
bin/
obj/
.vscode/
.vs/

# Test results (generated, not committed)
tests/results/
tests/results/*.json
tests/results/*.xml
tests/results/endpoint-audit-*.md
tests/results/endpoint-audit-*.csv
tests/results/auth-verification-*.md
tests/results/coverage-*.xml
tests/results/capture-*.log
TestResults/
testResults.xml

# Secrets + local config
*.secret
*.key
*.pem
*.pfx
*.passkey.json
shell-config.json
tests/.env.local
tests/.env.*.local
my-passkey.json

# Compiled ARM (generated from Bicep — committed intentionally but allow local regen)
# deploy/compiled/*.json      # uncomment to keep compiled out of git
deploy/compiled/*.tmp.json

# Solution staging (regenerated by tools/Build-SentinelSolution.ps1; release workflow rebuilds)
deploy/solution-staging/

# Function app zip (built by release workflow)
function-app.zip
xdrlograider-solution-*.zip

# Bicep build artifacts
*.bicepparam.json

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Editor
*.swp
*~

# Temp
*.tmp
*.log

# Internal/operational scratch (must never reach the public repo)
.protection-restore.json
.scratch/
NOTES.local.md
TODO.local.md

# Dev-only ad-hoc diagnostic tools (one-off root-cause investigation scripts) —
# kept off the public repo. Operator-facing tool is exactly:
#   Initialize-XdrLogRaiderAuth.ps1  (post-deploy secret upload, one-time)
# CI-facing tools (Validate-ArmJson, Preflight-Deployment, Build-Sentinel*)
# stay in tools/ for transparency.
# Internal verification + dev tooling (Post-DeploymentVerification.ps1,
# Run-LocalTests.ps1, Initialize-XdrLogRaiderSP.ps1, Test-ConnectorHealth.ps1)
# lives in .internal/tools/ — gitignored.
tools/Diagnose-*.ps1
tools/_*.ps1
tools/Prove-EndToEnd.ps1
