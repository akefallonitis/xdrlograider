  /admin/api/contentunderstanding/setting:
    get:
      summary: Get primary content understanding settings
      description: >-
        Retrieve the primary Content Understanding settings payload used to bootstrap the
        admin experience.
      operationId: ContentUnderstanding.GetSetting
      tags: [ContentUnderstanding]
      x-nodoc-operation-context:
        surface: Copilot > Settings > Content understanding
        workflows:
          - Open the main Content Understanding settings experience.
        headerProfile: m365-admin-homepage
        provenance:
          - source: module-validation
            confidence: high
            notes:
              - Validated against the checked-in M365Internals Content Understanding cmdlet.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: Primary Content Understanding settings payload. Exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
