  /fd/edgeenterpriseextensionsmanagement/api/version/update:
    get:
      summary: Get Edge update information
      description: >-
        Retrieve the current Edge update metadata used by the enterprise extensions
        management experience.
      operationId: Edge.GetVersionUpdate
      tags: [Edge]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: Edge update metadata payload. Exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
