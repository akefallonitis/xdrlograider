  /fd/edgeenterpriseextensionsmanagement/api/featureManagement/profiles:
    get:
      summary: List Edge feature management profiles
      description: >-
        Retrieve feature management profiles used by the Edge enterprise extensions
        management experience.
      operationId: Edge.ListFeatureManagementProfiles
      tags: [Edge]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: Feature management profiles payload. Exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
