  /fd/edgeenterpriseextensionsmanagement/api/featureManagement/shard:
    get:
      summary: Get Edge feature management shard
      description: >-
        Retrieve the shard assignment used by the Edge enterprise extensions
        management feature service.
      operationId: Edge.GetFeatureManagementShard
      tags: [Edge]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: Feature management shard payload. Exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
