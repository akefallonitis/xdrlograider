  /fd/OfficePolicyAdmin/v1.0/edge/policies/{id}/policywithpayload:
    get:
      summary: Get Edge policy with payload
      description: >-
        Retrieve a specific Edge browser policy together with its payload details.
      operationId: Edge.GetPolicyWithPayload
      tags: [Edge]
      parameters:
        - name: id
          in: path
          required: true
          description: The Edge policy identifier.
          schema:
            type: string
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: Edge policy payload detail. Exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound