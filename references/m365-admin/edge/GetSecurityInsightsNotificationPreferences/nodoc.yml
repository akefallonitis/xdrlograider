  /fd/edgeenterpriseextensionsmanagement/security-insights/notification-preferences:
    get:
      summary: Get Edge security insights notification preferences
      description: >-
        Retrieve notification preference settings used by the Edge security insights
        experience.
      operationId: Edge.GetSecurityInsightsNotificationPreferences
      tags: [Edge]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: Security insights notification preference payload. Exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
