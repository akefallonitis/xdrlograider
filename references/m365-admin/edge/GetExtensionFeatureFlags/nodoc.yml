  /fd/edgeenterpriseextensionsmanagement/api/config/featureflags:
    get:
      summary: Get Edge extension feature flags
      description: >-
        Retrieve feature flags used by the Edge enterprise extensions management
        experience.
      operationId: Edge.GetExtensionFeatureFlags
      tags: [Edge]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: Edge extension feature flag payload. Exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
