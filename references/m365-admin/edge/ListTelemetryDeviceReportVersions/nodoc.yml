  /fd/edgeenterpriseextensionsmanagement/api/telemetry/devices/reports/versions:
    get:
      summary: List Edge telemetry report versions
      description: >-
        Retrieve the available device telemetry report versions used by the Edge
        management reporting experience.
      operationId: Edge.ListTelemetryDeviceReportVersions
      tags: [Edge]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: Device report version payload. Exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
