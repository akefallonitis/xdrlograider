  /admin/api/settings/apps/officeonline:
    get:
      summary: Get M365 on the web settings
      description: >-
        Retrieve the Microsoft 365 on the web (Office Online) settings for the tenant (Org Settings > M365 on the web).
      operationId: AppSettings.GetOfficeOnline
      tags: [AppSettings]
      x-nodoc-operation-context:
        surface: Org settings > Services > Microsoft 365 on the web
        workflows:
          - Open Org settings, then Services, then Microsoft 365 on the web.
        headerProfile: m365-admin-office-online
        canonicalPath: /admin/api/settings/apps/officeonline
        pathAliases:
          - /admin/api/settings/apps/officeontheweb
        provenance:
          - source: module-validation
            confidence: high
            notes:
              - The checked-in M365Internals helper maps both Microsoft365OnTheWeb and OfficeOnline to this path.
          - source: live-portal-capture
            confidence: high
            notes:
              - Live navigation clicked the Microsoft 365 on the web row and issued `GET /admin/api/settings/apps/officeonline` with the OfficeOnline blade context.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                required:
                  - Enabled
                properties:
                  Enabled:
                    type: boolean
                additionalProperties: true
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
    post:
      summary: Set M365 on the web settings
      description: >-
        Update the Microsoft 365 on the web (Office Online) settings for the tenant.
      operationId: AppSettings.SetOfficeOnline
      tags: [AppSettings]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Merged Microsoft 365 on the web settings payload.
      x-nodoc-operation-context:
        surface: Org settings > Services > Microsoft 365 on the web
        workflows:
          - Open Org settings, then Services, then Microsoft 365 on the web.
        headerProfile: m365-admin-office-online
        canonicalPath: /admin/api/settings/apps/officeonline
        pathAliases:
          - /admin/api/settings/apps/officeontheweb
        requestShapes:
          - name: merged-office-online-settings
            contentType: application/json
            bodyKind: object-merge
            notes: The portal posts the merged settings document for this surface.
        provenance:
          - source: module-validation
            confidence: high
            notes:
              - The checked-in M365Internals helper maps both Microsoft365OnTheWeb and OfficeOnline to this path.
          - source: live-portal-capture
            confidence: high
            notes:
              - The live blade route was `#/Settings/Services/:/Settings/L1/OfficeOnline`, matching the canonical `officeonline` path.
      responses:
        '200':
          description: OK
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
