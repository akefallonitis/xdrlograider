  /fd/bsxcommerce/v1/moderncatalog/marketinggroups:
    get:
      summary: Get marketplace marketing groups
      description: >-
        Retrieve marketplace marketing groups and featured product bundles shown on the
        Microsoft 365 marketplace landing experience.
      operationId: Billing.GetMarketplaceMarketingGroups
      tags: [Billing]
      parameters:
        - name: languages
          in: query
          schema:
            type: string
        - name: market
          in: query
          schema:
            type: string
        - name: fieldsTemplate
          in: query
          schema:
            type: string
        - name: pricingAudience
          in: query
          schema:
            type: string
        - name: nationalCloud
          in: query
          schema:
            type: string
        - name: accountId
          in: query
          schema:
            type: string
        - name: orgId
          in: query
          schema:
            type: string
        - name: bgId
          in: query
          schema:
            type: string
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: Marketplace marketing groups payload. Exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
