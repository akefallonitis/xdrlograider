  /admin/api/tenant/o365activationusercounts:
    get:
      summary: Get O365 activation user counts
      description: >-
        Retrieve Office 365 activation user counts for the tenant.
      operationId: Tenant.GetO365ActivationUserCounts
      tags: [Tenant]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: O365 activation user counts. Exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
