  /admin/api/searchadminapi/searchintelligencehome/cards:
    get:
      summary: Get search intelligence home cards
      description: >-
        Retrieve the Search & intelligence landing-page card inventory used to render
        the Microsoft Search admin homepage.
      operationId: Search.GetSearchIntelligenceHomeCards
      tags: [Search]
      x-nodoc-operation-context:
        surface: Settings > Search & intelligence
        workflows:
          - Open the Search & intelligence landing page.
        headerProfile: m365-admin-microsoft-search
        provenance:
          - source: live-portal-capture
            confidence: high
            notes:
              - Verified on the signed-in `#/MicrosoftSearch` route during initial page load.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: Search intelligence homepage card inventory. Exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
