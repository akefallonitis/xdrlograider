  /admin/api/searchadminapi/UDTConnectorsSummary:
    get:
      summary: Get connectors summary
      description: >-
        Retrieve a summary of User-Defined Type (UDT) data connectors for Microsoft Search.
      operationId: Search.GetConnectorsSummary
      tags: [Search]
      parameters:
        - name: query
          in: query
          description: Search query string.
          schema:
            type: string
      x-nodoc-operation-context:
        surface: Settings > Search & intelligence
        workflows:
          - Open Search connectors and retrieve connector summary metrics.
        headerProfile: m365-admin-microsoft-search
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: UDT connectors summary. Exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
