  /fd/mssearchconnectors/v1.0/admin/connections/getStatistics:
    get:
      summary: Get connection statistics
      description: >-
        Retrieve statistics for Microsoft Search data connections including
        item counts, crawl status, and connection health.
      operationId: Search.GetConnectionStatistics
      tags: [Search]
      parameters:
        - name: query
          in: query
          description: Search query string.
          schema:
            type: string
      x-nodoc-operation-context:
        surface: Settings > Search & intelligence
        workflows:
          - Open Search connectors and inspect per-connection statistics.
        headerProfile: m365-admin-microsoft-search
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: Connection statistics. Exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
