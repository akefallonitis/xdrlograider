  /fd/substrateAcronyms/api/v1.0/GetTenantAdminAcronymsFromSds:
    post:
      summary: Get tenant acronyms from substrate
      description: >-
        Retrieve tenant-defined acronyms surfaced by the Search & intelligence experience.
      operationId: Search.GetTenantAcronyms
      tags: [Search]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Acronym query payload. The live portal posted an empty JSON object.
      x-nodoc-operation-context:
        surface: Settings > Search & intelligence
        workflows:
          - Open the Search & intelligence landing page and load acronym-backed content.
        headerProfile: m365-admin-microsoft-search
        requestShapes:
          - name: search-acronyms-default
            contentType: application/json
            bodyKind: empty-object
            notes: The live homepage request posted `{}`.
        provenance:
          - source: live-portal-capture
            confidence: high
            notes:
              - Verified on the signed-in `#/MicrosoftSearch` route.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: Tenant acronym payload. Exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound