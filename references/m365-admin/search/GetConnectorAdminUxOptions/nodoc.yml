  /fd/mssearchconnectors/v1.0/admin/AdminUxOptions:
    get:
      summary: Get search connector admin UX options
      description: >-
        Retrieve connector capability flags and UX options surfaced by the Search & intelligence landing page.
      operationId: Search.GetConnectorAdminUxOptions
      tags: [Search]
      x-nodoc-operation-context:
        surface: Settings > Search & intelligence
        workflows:
          - Open the Search & intelligence landing page and load connector-related tiles.
        headerProfile: m365-admin-microsoft-search
        provenance:
          - source: live-portal-capture
            confidence: high
            notes:
              - Verified on the signed-in `#/MicrosoftSearch` route during initial homepage hydration.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: Connector admin UX options payload. Exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
