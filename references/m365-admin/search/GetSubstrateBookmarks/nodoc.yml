  /fd/substrateBookmarks/api/v3/management/bookmarks:
    post:
      summary: Get substrate bookmarks
      description: >-
        Retrieve bookmark management data surfaced by the Search & intelligence experience.
      operationId: Search.GetSubstrateBookmarks
      tags: [Search]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Bookmark query payload. The live portal posted an empty JSON object.
      x-nodoc-operation-context:
        surface: Settings > Search & intelligence
        workflows:
          - Open the Search & intelligence landing page and load bookmark-backed cards.
        headerProfile: m365-admin-microsoft-search
        requestShapes:
          - name: search-bookmarks-default
            contentType: application/json
            bodyKind: empty-object
            notes: The live homepage request posted `{}`.
        provenance:
          - source: live-portal-capture
            confidence: high
            notes:
              - Verified on the signed-in `#/MicrosoftSearch` route.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: Substrate bookmarks payload. Exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
