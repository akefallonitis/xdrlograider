  /config/v1/SecurityMarketplaceClient/1.0.0:
    get:
      tags: [Store]
      summary: Get Security Store client configuration
      description: Retrieves ECS configuration used by the Security Store page, including marketplace search and ranking settings.
      operationId: SecurityCopilotPortal.Store.GetClientConfiguration
      parameters:
        - $ref: '#/components/parameters/SecurityService'
        - $ref: '#/components/parameters/Environment'
      responses:
        '200':
          description: Store client configuration returned successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MarketplaceClientConfigResponse'
