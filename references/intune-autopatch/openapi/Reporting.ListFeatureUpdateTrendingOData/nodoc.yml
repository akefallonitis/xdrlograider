  /unified-reporting/odata/1.0/WindowsFeatureUpdatesTrending:
    get:
      tags: [Reporting]
      summary: List feature update trend points via URS
      description: |
        Returns the alternate URS/OData model for feature update trend data. This path was
        confirmed live on `windowsFeatureUpdatesReactView` and separately confirmed with a safe
        GET probe.
      operationId: IntuneAutopatch.Reporting.ListFeatureUpdateTrendingOData
      parameters:
        - $ref: '#/components/parameters/top'
        - $ref: '#/components/parameters/filter'
        - $ref: '#/components/parameters/orderBy'
      responses:
        '200':
          description: Feature update trend rows returned successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WindowsFeatureUpdateTrendingODataCollection'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
