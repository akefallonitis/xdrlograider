  /unified-reporting/odata/1.0/WindowsFeatureUpdatesSummaryRelease:
    get:
      tags: [Reporting]
      summary: List feature update release summaries via URS
      description: |
        Returns the alternate URS/OData model for feature update release summaries. The path was
        recovered from the report bundle and confirmed with a safe GET probe.
      operationId: IntuneAutopatch.Reporting.ListFeatureUpdateSummaryReleaseOData
      parameters:
        - $ref: '#/components/parameters/top'
        - $ref: '#/components/parameters/orderBy'
      responses:
        '200':
          description: Feature update release summaries returned successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WindowsFeatureUpdateSummaryReleaseODataCollection'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
