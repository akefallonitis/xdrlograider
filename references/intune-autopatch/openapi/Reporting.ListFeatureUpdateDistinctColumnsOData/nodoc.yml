  /unified-reporting/odata/1.0/WindowsFeatureUpdatesDistinctColumns:
    get:
      tags: [Reporting]
      summary: Get feature update distinct columns via URS
      description: |
        Returns the alternate URS/OData model for feature update distinct-column values. The path
        was recovered from the report bundle and confirmed with a safe GET probe.
      operationId: IntuneAutopatch.Reporting.ListFeatureUpdateDistinctColumnsOData
      parameters:
        - $ref: '#/components/parameters/top'
      responses:
        '200':
          description: Feature update distinct-column values returned successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WindowsFeatureUpdateDistinctColumnsODataCollection'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
