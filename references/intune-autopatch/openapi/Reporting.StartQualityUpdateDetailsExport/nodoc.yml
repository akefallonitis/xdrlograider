  /reporting/reports/v2/deviceAccounting/wqu/detailsExport:
    post:
      tags: [Reporting]
      summary: Start a quality update details export
      description: |
        Starts the quality update details export flow. The route and request body were recovered
        from the report bundle; the helper expects a `trackingId` response, then polls
        `GET /reporting/reports/v1/export/status/` with
        `queryName=DeviceQualityUpdatesDetailsQuery` until an export SAS URL is returned.
      operationId: IntuneAutopatch.Reporting.StartQualityUpdateDetailsExport
      parameters:
        - $ref: '#/components/parameters/reportApiVersion2'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QualityUpdateDetailsExportRequest'
      responses:
        '200':
          description: Quality update export job started successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportExportStartResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
