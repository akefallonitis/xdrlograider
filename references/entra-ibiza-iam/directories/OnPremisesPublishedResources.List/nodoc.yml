  /Directories/{tenantId}/OnPremisesPublishedResources:
    parameters:
      - $ref: common.yml#/components/parameters/XMsClientRequestId
      - $ref: '#/components/parameters/tenantId'
    get:
      summary: List on premises published resources
      description: >-
        List on premises published resources.
      operationId: Directories.OnPremisesPublishedResources.List
      tags: [Directory]
      responses:
        '200':
          $ref: common.yml#/components/responses/UnknownResponse
        4XX:
          $ref: common.yml#/components/responses/ClientException
