  /MultiFactorAuthentication/DownloadServerStatusReport:
    parameters:
      - $ref: common.yml#/components/parameters/XMsClientRequestId
    get:
      summary: Download MFA server status report
      description: >-
        Download MFA server status report.
      operationId: Mfa.ServerStatusReport.Download
      tags: [Multi Factor Authentication]
      responses:
        '200':
          $ref: common.yml#/components/responses/UnknownResponse
        4XX:
          $ref: common.yml#/components/responses/ClientException
