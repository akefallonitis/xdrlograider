  /MultiFactorAuthentication/DownloadAuthenticationReport:
    parameters:
      - $ref: common.yml#/components/parameters/XMsClientRequestId
    post:
      summary: Download MFA Authentication report
      description: >-
        Download MFA Authentication report.
      operationId: Mfa.AuthenticationReport.Download
      tags: [Multi Factor Authentication]
      responses:
        '200':
          $ref: common.yml#/components/responses/UnknownResponse
        4XX:
          $ref: common.yml#/components/responses/ClientException
