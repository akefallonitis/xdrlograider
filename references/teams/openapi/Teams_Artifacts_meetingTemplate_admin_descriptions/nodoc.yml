  /Teams.Artifacts/meetingTemplate/admin/descriptions:
    servers:
      - url: https://api.interfaces.records.teams.microsoft.com
        description: Teams interfaces records service
    get:
      tags: [TeamsTemplates]
      summary: List Teams meeting template descriptions
      description: |
        Meeting template metadata observed from Teams meeting and template administration pages.
      responses:
        '200':
          description: Meeting template description payload.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericObject'
        '401':
          $ref: '#/components/responses/Unauthorized'