  /api/mt/part/{partition}/beta/users/useraggregatesettings:
    servers:
      - url: https://teams.microsoft.com
        description: Teams core service host
    post:
      tags: [TeamsCore]
      summary: Update Teams admin user aggregate settings
      description: |
        Write observed while persisting aggregate display or filter settings for user-focused Teams admin
        experiences.
      parameters:
        - $ref: '#/components/parameters/Partition'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenericObject'
      responses:
        '200':
          description: Aggregate settings update result.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericObject'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  # api.interfaces.records.teams.microsoft.com