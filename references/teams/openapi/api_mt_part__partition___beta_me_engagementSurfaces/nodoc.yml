  /api/mt/part/{partition}//beta/me/engagementSurfaces:
    servers:
      - url: https://teams.microsoft.com
        description: Teams core service host
    get:
      tags: [TeamsCore]
      summary: Get regional admin engagement surface state
      description: |
        Regionalized engagement surface lookup observed after the portal resolved the current user's
        partition. The live request preserved the double slash before `beta`.
      parameters:
        - $ref: '#/components/parameters/Partition'
      responses:
        '200':
          description: Regional engagement surface payload.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericObject'
        '401':
          $ref: '#/components/responses/Unauthorized'