  /api/mt/part/{partition}/beta/admin/tenantStagedApps:
    servers:
      - url: https://teams.microsoft.com
        description: Teams core service host
    get:
      tags: [TeamsCore]
      summary: List tenant staged Teams apps
      description: |
        Returns staged app records used by Teams app management surfaces.
      parameters:
        - $ref: '#/components/parameters/Partition'
      responses:
        '200':
          description: Staged app collection.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericObject'
        '401':
          $ref: '#/components/responses/Unauthorized'