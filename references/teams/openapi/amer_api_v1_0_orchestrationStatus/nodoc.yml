  /amer/api/v1.0/orchestrationStatus:
    servers:
      - url: https://flworchestrator.teams.microsoft.com
        description: Frontline orchestration backend
    get:
      tags: [TeamsFrontline]
      summary: Get the Frontline orchestration status
      description: |
        Orchestration status lookup used by Frontline setup and insights pages.
      responses:
        '200':
          description: Orchestration status payload.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericObject'
        '401':
          $ref: '#/components/responses/Unauthorized'