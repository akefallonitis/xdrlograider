  /api/v1/clientHealth/clientHealthTopIssues:
    servers:
      - url: https://noneu-admin.teams.microsoft.com
        description: Regional Teams admin service
    get:
      tags: [RegionalAdmin]
      summary: List top Teams client health issues
      description: |
        Top-issues summary route observed from `Teams client health`.
      responses:
        '200':
          description: Top client health issues payload.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericObject'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'