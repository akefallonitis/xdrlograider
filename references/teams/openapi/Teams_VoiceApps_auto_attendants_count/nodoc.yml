  /Teams.VoiceApps/auto-attendants/count:
    servers:
      - url: https://api.interfaces.records.teams.microsoft.com
        description: Teams interfaces records service
    get:
      tags: [TeamsVoiceApps]
      summary: Get the Teams auto attendant count
      description: |
        Count helper used by Teams voice administration pages before loading full auto attendant data.
      responses:
        '200':
          description: Auto attendant count payload.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericObject'
        '401':
          $ref: '#/components/responses/Unauthorized'