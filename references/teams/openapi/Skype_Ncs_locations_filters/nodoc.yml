  /Skype.Ncs/locations/filters:
    servers:
      - url: https://api.interfaces.records.teams.microsoft.com
        description: Teams interfaces records service
    get:
      tags: [InterfacesRecords]
      summary: List Teams location filters
      description: |
        Returns location filters from the Teams network and civic services backend.
      responses:
        '200':
          description: Location filter collection.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/GenericArrayItem'
        '401':
          $ref: '#/components/responses/Unauthorized'