  /amer/api/v1/Security/Rules/InactiveTeams/View-Details:
    servers:
      - url: https://collabinsights.teams.cloud.microsoft
        description: Teams collaboration insights backend
    get:
      tags: [TeamsCollaborationInsights]
      summary: View inactive Teams rule details
      description: |
        Collaboration insight detail route discovered from the Teams security rule surface. The retained
        tenant returned `403 Forbidden`, which suggests the route requires additional permissions or a
        feature flag.
      responses:
        '200':
          description: Inactive Teams rule detail payload.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericObject'
        '403':
          $ref: '#/components/responses/Forbidden'
        '401':
          $ref: '#/components/responses/Unauthorized'

  # teams.cloud.microsoft/api