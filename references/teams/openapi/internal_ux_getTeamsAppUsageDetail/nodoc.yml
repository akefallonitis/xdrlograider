  /internal/ux/getTeamsAppUsageDetail:
    servers:
      - url: https://reportsncu.office.com
        description: Teams premium report UX host
    get:
      tags: [ReportsUx]
      summary: Get Teams app usage detail
      description: |
        Detailed app-usage reporting route observed during the second-pass report interaction crawl.
      parameters:
        - $ref: '#/components/parameters/TenantIdQuery'
        - $ref: '#/components/parameters/Aggregate'
      responses:
        '200':
          description: Teams app usage detail payload.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ODataValueCollection'
        '401':
          $ref: '#/components/responses/Unauthorized'