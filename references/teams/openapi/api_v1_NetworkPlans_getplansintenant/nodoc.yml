  /api/v1/NetworkPlans/getplansintenant:
    servers:
      - url: https://noneu-admin.teams.microsoft.com
        description: Regional Teams admin service
    get:
      tags: [RegionalAdmin]
      summary: List tenant network plans
      description: |
        Network plan inventory observed from `Planning > Network planner`. The retained tenant returned
        `204 No Content`.
      responses:
        '204':
          $ref: '#/components/responses/NoContent'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'