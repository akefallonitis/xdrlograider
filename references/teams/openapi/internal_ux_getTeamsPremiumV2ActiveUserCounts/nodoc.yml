  /internal/ux/getTeamsPremiumV2ActiveUserCounts:
    servers:
      - url: https://reportsncu.office.com
        description: Teams premium report UX host
    get:
      tags: [ReportsUx]
      summary: Get Teams Premium active user counts
      description: |
        Premium reporting endpoint observed from the Teams admin dashboard.
      parameters:
        - $ref: '#/components/parameters/TenantIdQuery'
        - $ref: '#/components/parameters/Aggregate'
      responses:
        '200':
          description: Active user counts.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ODataValueCollection'
        '401':
          $ref: '#/components/responses/Unauthorized'