  /api/mt/part/{partition}/beta/users/appsCatalog:
    servers:
      - url: https://teams.microsoft.com
        description: Teams core service host
    get:
      tags: [TeamsCore]
      summary: Get the current user's Teams apps catalog view
      description: |
        Partitioned Teams apps catalog lookup used by the admin center. The route was confirmed with a
        `200` response, but an exact response schema was not preserved in the retained sample body.
      parameters:
        - $ref: '#/components/parameters/Partition'
      responses:
        '200':
          description: Apps catalog payload. Exact schema pending.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericObject'
        '401':
          $ref: '#/components/responses/Unauthorized'