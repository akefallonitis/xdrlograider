  /Teams.Templates/api/teamtemplates/v1.0/{localeCode}:
    servers:
      - url: https://api.interfaces.records.teams.microsoft.com
        description: Teams interfaces records service
    get:
      tags: [TeamsTemplates]
      summary: List Teams team templates
      description: |
        Team template inventory observed from `Teams > Team templates`.
      parameters:
        - $ref: '#/components/parameters/LocaleCode'
      responses:
        '200':
          description: Team template collection.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericObject'
        '401':
          $ref: '#/components/responses/Unauthorized'