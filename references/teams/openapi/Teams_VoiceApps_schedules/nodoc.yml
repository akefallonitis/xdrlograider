  /Teams.VoiceApps/schedules:
    servers:
      - url: https://api.interfaces.records.teams.microsoft.com
        description: Teams interfaces records service
    get:
      tags: [TeamsVoiceApps]
      summary: List Teams voice-app schedules
      description: |
        Schedule inventory used by Teams auto attendant and call queue administration surfaces.
      responses:
        '200':
          description: Voice-app schedule collection.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericObject'
        '401':
          $ref: '#/components/responses/Unauthorized'