  /amer/api/reports/v1.0/flw/orchestration/usage:
    servers:
      - url: https://flworchestrator.teams.microsoft.com
        description: Frontline orchestration backend
    post:
      tags: [TeamsFrontline]
      summary: Query Frontline orchestration usage
      description: |
        Usage report query observed from Frontline usage insights. The body shape depends on the selected
        report state and should be treated as portal-context dependent.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenericObject'
      x-nodoc-operation-context:
        surface: Frontline > Usage insights
        workflows:
          - Open Frontline usage insights and change the selected report state or filters before querying orchestration usage.
        headerProfile: teams-service-bearer
        requestShapes:
          - name: frontline-usage-query
            contentType: application/json
            bodyKind: query-object
            notes: The request body varies with the selected report state, filter set, and time window shown in the Frontline usage experience.
        availability:
          level: request-dependent
          notes:
            - The returned data shape depends on the active report slice chosen in the portal.
        provenance:
          - source: live-portal-capture
            confidence: high
            notes:
              - Verified from authenticated Frontline usage-insights browsing against the flworchestrator regional backend.
      responses:
        '200':
          description: Frontline orchestration usage result.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericObject'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'