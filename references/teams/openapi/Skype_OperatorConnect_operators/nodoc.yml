  /Skype.OperatorConnect/operators:
    servers:
      - url: https://api.interfaces.records.teams.microsoft.com
        description: Teams interfaces records service
    get:
      tags: [TeamsTelephony]
      summary: List Operator Connect operators
      description: |
        Operator inventory used by Teams voice routing and Operator Connect administration pages.
      responses:
        '200':
          description: Operator collection.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericObject'
        '401':
          $ref: '#/components/responses/Unauthorized'