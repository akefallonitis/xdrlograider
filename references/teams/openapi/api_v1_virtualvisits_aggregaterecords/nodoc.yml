  /api/v1/virtualvisits/aggregaterecords:
    servers:
      - url: https://amer-ehrconnectorsvc.teams.microsoft.com
        description: Regional virtual visits backend
    get:
      tags: [RegionalEhrConnector]
      summary: Query virtual visits aggregate records
      description: |
        Virtual visits analytics query observed from the dashboard widget. The retained live request
        returned `422 Unprocessable Content`, which strongly suggests the route is real but sensitive to
        exact filter, ordering, or other dashboard context.
      parameters:
        - $ref: '#/components/parameters/Filter'
        - $ref: '#/components/parameters/OrderBy'
        - $ref: '#/components/parameters/Top'
      responses:
        '200':
          description: Aggregate records result. Exact schema pending.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericObject'
        '422':
          $ref: '#/components/responses/UnprocessableContent'
        '401':
          $ref: '#/components/responses/Unauthorized'

  # vamonetization.teams.microsoft.com