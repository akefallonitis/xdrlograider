  /Skype.Policy/assignments/operationsall:
    servers:
      - url: https://api.interfaces.records.teams.microsoft.com
        description: Teams interfaces records service
    get:
      tags: [TeamsPolicyAssignments]
      summary: List Teams policy assignment operations
      description: |
        Policy-assignment operation metadata observed while drilling into policy assignment and user
        policy pages.
      responses:
        '200':
          description: Policy-assignment operation metadata.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericObject'
        '401':
          $ref: '#/components/responses/Unauthorized'