  /api/v1/clientHealth/clientHealthDetails:
    servers:
      - url: https://noneu-admin.teams.microsoft.com
        description: Regional Teams admin service
    get:
      tags: [RegionalAdmin]
      summary: Get Teams client health details
      description: |
        Client health detail route observed from `Teams client health`.
      responses:
        '200':
          description: Client health detail payload.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericObject'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'