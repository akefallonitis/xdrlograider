  /amer/api/v1.0/hybridTeams/deployedLocationsCount:
    servers:
      - url: https://flworchestrator.teams.microsoft.com
        description: Frontline orchestration backend
    get:
      tags: [TeamsFrontline]
      summary: Get the Frontline deployed locations count
      description: |
        Deployed-location count observed from Frontline hybrid teams administration pages.
      responses:
        '200':
          description: Deployed location count payload.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericObject'
        '401':
          $ref: '#/components/responses/Unauthorized'