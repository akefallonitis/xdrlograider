  /api/v2/tags/enroll:
    servers:
      - url: https://admin.devicemgmt.teams.microsoft.com
        description: Teams device management data host
    get:
      tags: [TeamsDeviceManagement]
      summary: List Teams device enrollment tags
      description: |
        Enrollment tag inventory used by Teams devices management pages.
      responses:
        '200':
          description: Device enrollment tag payload.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericObject'
        '401':
          $ref: '#/components/responses/Unauthorized'