{
  "httpStatus": 200,
  "successKind": "live",
  "rowCount": 1,
  "errorText": "",
  "responseShape": "wrapper:value",
  "sample": {
    "@odata.context": "https://admin.exchange.microsoft.com/beta/$metadata#Edm.String",
    "value": "<REDACTED-JWT>"
  },
  "_redaction_note": "Original Phase 0 capture contained a delegated OAuth access token (JWT) — redacted before commit. The captured token's claims, audience, and signed payload are not required for v0.1.0 generators; only the response shape (wrapper:value) and HTTP 200 are consumed."
}
