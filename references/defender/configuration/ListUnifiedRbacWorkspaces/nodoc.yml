  /mtp/urbacConfiguration/gw/unifiedrbac/configuration/workspaces:
    get:
      summary: List unified RBAC workspaces
      description: >-
        Retrieve the Defender workspaces and scope metadata used by the unified RBAC
        configuration experience.
      operationId: Configuration.ListUnifiedRbacWorkspaces
      tags: [Configuration]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: pending - unified RBAC workspaces response
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
