  /mdc/views/recommendations/schema:
    post:
      summary: Get MDC recommendations schema
      description: >-
        Retrieve the schema metadata used by the Defender portal to render Microsoft
        Defender for Cloud recommendation grids.
      operationId: ExposureManagement.GetMdcRecommendationSchema
      tags: [Exposure Management]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
              description: pending - MDC recommendation schema request payload
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: pending - MDC recommendation schema response
        '401':
          $ref: common.yml#/components/responses/Unauthorized
        '403':
          $ref: common.yml#/components/responses/Forbidden
        '404':
          $ref: common.yml#/components/responses/NotFound
