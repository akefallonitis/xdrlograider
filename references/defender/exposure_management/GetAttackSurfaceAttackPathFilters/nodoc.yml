  /mtp/xspmatlas/attacksurface/attackpaths/filters:
    get:
      summary: Get attack path filter options
      description: >-
        Retrieve the available filter options for attack surface attack paths. Bundle-discovered;
        direct GET probing returned a contextual 500 without page-specific parameters.
      operationId: ExposureManagement.GetAttackSurfaceAttackPathFilters
      tags: [Exposure Management]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: Attack path filter options — exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized
        '403':
          $ref: common.yml#/components/responses/Forbidden
        '500':
          description: Server error — may require page-specific query parameters not captured during probing.
