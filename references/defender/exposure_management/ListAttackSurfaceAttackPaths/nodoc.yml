  /mtp/xspmatlas/attacksurface/attackpaths:
    get:
      summary: List attack surface attack paths
      description: >-
        Retrieve a list of active attack paths from the XSPMAtlas attack surface. Bundle-discovered;
        direct GET probing returned a contextual 500 without page-specific parameters.
        Related POST query surfaces are documented at /mtp/xspmatlas/attacksurface/query.
      operationId: ExposureManagement.ListAttackSurfaceAttackPaths
      tags: [Exposure Management]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: Attack paths list — exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized
        '403':
          $ref: common.yml#/components/responses/Forbidden
        '500':
          description: Server error — may require page-specific query parameters not captured during probing.
