  /mdc/views/recommendations/items:
    post:
      summary: List MDC recommendation items
      description: >-
        Retrieve the Microsoft Defender for Cloud recommendation items rendered by the
        Defender portal's cloud posture recommendations experience.
      operationId: ExposureManagement.ListMdcRecommendationItems
      tags: [Exposure Management]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
              description: pending - MDC recommendations items query payload
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: pending - MDC recommendations items response
        '401':
          $ref: common.yml#/components/responses/Unauthorized
        '403':
          $ref: common.yml#/components/responses/Forbidden
        '404':
          $ref: common.yml#/components/responses/NotFound
