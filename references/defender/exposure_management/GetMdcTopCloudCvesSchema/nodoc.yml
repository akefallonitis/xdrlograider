  /mdc/views/vulnerabilities/topCloudCVEsSchema:
    post:
      summary: Get MDC top cloud CVE schema
      description: >-
        Retrieve the schema metadata for the Defender portal's Microsoft Defender for
        Cloud top cloud CVE grid.
      operationId: ExposureManagement.GetMdcTopCloudCvesSchema
      tags: [Exposure Management]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
              description: pending - MDC top cloud CVE schema request payload
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: pending - MDC top cloud CVE schema response
        '401':
          $ref: common.yml#/components/responses/Unauthorized
        '403':
          $ref: common.yml#/components/responses/Forbidden
        '404':
          $ref: common.yml#/components/responses/NotFound
