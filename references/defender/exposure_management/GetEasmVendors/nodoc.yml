  /mtp/posture/oversight/easm/vendors:
    get:
      summary: Get EASM vendor list
      description: >-
        Retrieve the list of External Attack Surface Management (EASM) vendors available
        for the tenant. Bundle-discovered; direct GET probing returned a contextual 500
        without page-specific parameters.
      operationId: ExposureManagement.GetEasmVendors
      tags: [Exposure Management]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: EASM vendor list — exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized
        '403':
          $ref: common.yml#/components/responses/Forbidden
        '500':
          description: Server error — may require page-specific query parameters not captured during probing.
