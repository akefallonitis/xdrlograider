  /mtp/xspmatlas/attacksurface/chokepoints/list:
    get:
      summary: List attack surface choke points
      description: >-
        Retrieve a list of choke points from the XSPMAtlas attack surface. Bundle-discovered;
        direct GET probing returned a contextual 500 without page-specific parameters.
        Compare with the legacy XSPM endpoint at /mtp/xspm/chokepoints (POST).
      operationId: ExposureManagement.ListAttackSurfaceChokepoints
      tags: [Exposure Management]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: Choke points list — exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized
        '403':
          $ref: common.yml#/components/responses/Forbidden
        '500':
          description: Server error — may require page-specific query parameters not captured during probing.
