  /mtp/posture/oversight/metrics/ids:
    get:
      summary: Get posture metric IDs
      description: >-
        Retrieve the list of available posture metric identifiers. Bundle-discovered;
        direct GET probing returned a contextual 500 without page-specific parameters.
      operationId: ExposureManagement.GetPostureOversightMetricIds
      tags: [Exposure Management]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: Metric ID list — exact schema pending.
        '401':
          $ref: common.yml#/components/responses/Unauthorized
        '403':
          $ref: common.yml#/components/responses/Forbidden
        '500':
          description: Server error — may require page-specific query parameters not captured during probing.
