  /mdc/views/recommendations/recommendationsMetrics:
    post:
      summary: Get MDC recommendation metrics
      description: >-
        Retrieve aggregate recommendation metrics for the Defender portal's Microsoft
        Defender for Cloud posture views.
      operationId: ExposureManagement.GetMdcRecommendationMetrics
      tags: [Exposure Management]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
              description: pending - MDC recommendation metrics query payload
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: pending - MDC recommendation metrics response
        '401':
          $ref: common.yml#/components/responses/Unauthorized
        '403':
          $ref: common.yml#/components/responses/Forbidden
        '404':
          $ref: common.yml#/components/responses/NotFound
