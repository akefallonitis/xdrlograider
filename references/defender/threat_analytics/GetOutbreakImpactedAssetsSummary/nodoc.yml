  /mtp/threatAnalytics/outbreaks/v2/{OutbreakId}/impactedAssetsSummary:
    get:
      summary: Get outbreak impacted assets summary
      description: >-
        Retrieve the impacted asset summary tiles for a Threat Analytics report detail page.
      operationId: ThreatAnalytics.GetOutbreakImpactedAssetsSummary
      tags: [Threat Analytics]
      parameters:
        - name: OutbreakId
          in: path
          required: true
          description: The Threat Analytics outbreak identifier.
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: pending - impacted assets summary
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
