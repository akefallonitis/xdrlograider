  /mcas/cas/api/v1/app_connectors/dashboard/get_instance_count_by_app:
    get:
      summary: Get app connector instance count by app
      description: >-
        Retrieve the number of connected instances grouped by app for the app
        connectors dashboard. Discovered during the Defender for Cloud Apps
        full-nav capture.
      operationId: CloudApps.GetAppConnectorInstanceCountByApp
      tags: [CloudApps]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: pending
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
