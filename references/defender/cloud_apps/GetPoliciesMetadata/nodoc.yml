  /mcas/cas/api/v1/policies/metadata:
    get:
      summary: Get policies metadata
      description: >-
        Retrieve filter and column metadata for the Cloud Apps policies grid.
        Discovered during the Defender for Cloud Apps full-nav capture.
      operationId: CloudApps.GetPoliciesMetadata
      tags: [CloudApps]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: pending
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
