  /mcas/cas/api/v1/standalone_entities/count:
    post:
      summary: Get standalone entities count
      description: >-
        Retrieve the count of standalone entities matching the current filter
        criteria. Discovered during the Defender for Cloud Apps full-nav
        capture.
      operationId: CloudApps.GetStandaloneEntitiesCount
      tags: [CloudApps]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              description: pending - standalone entity count query
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: pending
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
