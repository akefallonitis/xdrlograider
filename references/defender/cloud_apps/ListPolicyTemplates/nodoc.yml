  /mcas/cas/api/v1/policy_templates_inmemo:
    post:
      summary: List policy templates
      description: >-
        Retrieve built-in policy templates available in Defender for Cloud Apps.
        Discovered during the Defender for Cloud Apps full-nav capture.
      operationId: CloudApps.ListPolicyTemplates
      tags: [CloudApps]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              description: pending - policy template query envelope
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: pending
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
