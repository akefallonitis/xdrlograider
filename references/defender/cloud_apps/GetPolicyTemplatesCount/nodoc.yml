  /mcas/cas/api/v1/policy_templates_inmemo/count:
    post:
      summary: Get policy templates count
      description: >-
        Retrieve the count of policy templates matching the current query.
        Discovered during the Defender for Cloud Apps full-nav capture.
      operationId: CloudApps.GetPolicyTemplatesCount
      tags: [CloudApps]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              description: pending - policy template count query
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: pending
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
