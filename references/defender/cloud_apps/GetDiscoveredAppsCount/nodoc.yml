  /mcas/cas/api/v1/discovery/discovered_apps/count:
    post:
      summary: Get discovered apps count
      description: >-
        Retrieve the total number of discovered apps that match the current Cloud
        Discovery filter set. Observed live posting paging and stream parameters
        and returning a `total` count envelope.
      operationId: CloudApps.GetDiscoveredAppsCount
      tags: [CloudApps]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: pending - discovered apps count query
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: pending - discovered apps count response
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
