  /mcas/cas/api/v1/policy_templates_inmemo/metadata:
    get:
      summary: Get policy templates metadata
      description: >-
        Retrieve filter and column metadata for the policy templates grid.
        Discovered during the Defender for Cloud Apps full-nav capture.
      operationId: CloudApps.GetPolicyTemplatesMetadata
      tags: [CloudApps]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: pending
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
