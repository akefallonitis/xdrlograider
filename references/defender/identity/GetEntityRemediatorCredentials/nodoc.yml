  /aatp/odata/EntityRemediatorCredentials:
    get:
      summary: Get entity remediator credentials
      description: >-
        Retrieve the entity remediator credentials configuration using the OData endpoint.
      operationId: Identity.GetEntityRemediatorCredentials
      tags: [Identity]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: pending - entity remediator credentials
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
