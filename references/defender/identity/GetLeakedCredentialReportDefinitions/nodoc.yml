  /mdi/identity/userapiservice/pdProtection/reportDefinitions/LeakedCredentials:
    get:
      summary: Get leaked-credential report definitions
      description: >-
        Retrieve the password-protection report definitions used by the Defender portal's
        Leaked Credentials view.
      operationId: Identity.GetLeakedCredentialReportDefinitions
      tags: [Identity]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  ActiveDirectory:
                    type: object
                    additionalProperties:
                      type: string
                  AzureActiveDirectory:
                    type: object
                    additionalProperties:
                      type: string
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
