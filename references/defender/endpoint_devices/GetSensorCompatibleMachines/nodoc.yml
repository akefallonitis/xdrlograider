  /mtp/mdi/tri/defensor/onboarding/devices/sensor_compatible_machines:
    get:
      summary: Get MDI sensor-compatible machines
      description: >-
        Retrieve the list of machines compatible with MDI sensor deployment
        for identity protection onboarding.
      operationId: EndpointDevices.GetSensorCompatibleMachines
      tags: [Endpoint Devices]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: pending - sensor compatible machines list
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
