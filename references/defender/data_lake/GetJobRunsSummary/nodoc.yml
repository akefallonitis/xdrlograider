  /securityplatform/jobs/runs/summary:
    get:
      summary: Get job runs summary
      description: >-
        Retrieve aggregate counters for configured jobs and recent job runs in the
        security platform data lake experience.
      operationId: DataLake.GetJobRunsSummary
      tags: [Data Lake]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  totalJobs:
                    type: integer
                  enabledJobs:
                    type: integer
                  completedRuns:
                    type: integer
                  inProgressRuns:
                    type: integer
                  recentHealthIssues:
                    type: integer
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
