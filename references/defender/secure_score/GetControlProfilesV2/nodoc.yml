  /mtp/secureScore/security/secureScoreControlProfilesV2:
    get:
      summary: Get secure score control profiles V2
      description: >-
        Retrieve the secure score control profiles using the V2 endpoint,
        including improvement actions and their status.
      operationId: SecureScore.GetControlProfilesV2
      tags: [Secure Score]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                description: pending - secure score control profiles V2 data
        '401':
          $ref: common.yml#/components/responses/Unauthorized

        '403':
          $ref: common.yml#/components/responses/Forbidden

        '404':
          $ref: common.yml#/components/responses/NotFound
