id: 9d7af2b3-4c6b-4d8f-e1a3-7b6c9f4d2e11
name: MDE after-hours drift events
description: |
  Compliance + pipeline drift events that occurred outside business hours
  (0900-1700 UTC) or on weekends. Attackers prefer making config changes
  off-hours to avoid review-window oversight.

  Workbook time-pickers can override the lookback/window via
  `{TimeRange:value}` — the underlying parsers are parameterized.

  Tenant-time-zone caveat: this query uses UTC. If your tenant operates in
  a non-UTC time zone, adjust 9/17 to your local business hours OR convert
  TimeGenerated to local TZ before the comparison.
author: Alex Kefallonitis
version: 1.1.0
tags:
  - Compliance
  - Drift
  - DefenderXDR
requiredDataConnectors:
  - connectorId: XdrLogRaiderInternal
    dataTypes:
      - MDE_Heartbeat_CL
tactics:
  - DefenseEvasion
relevantTechniques:
  - T1562
query: |
  union (MDE_Drift_P0Compliance(7d, 1h)), (MDE_Drift_P1Pipeline(7d, 30m))
  | extend HourOfDay = datetime_part("Hour", TimeGenerated)
  | extend DayOfWeek = dayofweek(TimeGenerated)
  | where HourOfDay < 9 or HourOfDay >= 17 or DayOfWeek == time(0d) or DayOfWeek == time(6d)
