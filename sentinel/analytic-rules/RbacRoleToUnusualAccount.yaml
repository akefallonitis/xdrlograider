id: 0ae1c3d4-5f78-4a9c-b2d4-8e7f0c5a3b08
name: MDE RBAC role to unusual account
description: |
  A Defender unified-RBAC role assignment was added in the last 24h. Fires on
  the *principal* identity fields specifically (not on every nested role
  attribute) so this rule alerts on "who got a new role" rather than every
  role-definition tweak. The FieldName filter uses exact-match (not substring)
  to avoid false positives on benign role re-evaluations.

  Operator response (suggested):
    1. Verify the principalId from the Drift row resolves to a known + sanctioned admin account
    2. If unknown account: investigate via SigninLogs cross-reference + recent privilege grants
    3. If sanctioned: tag the rule's tunable threshold to suppress this principal for 30d
severity: High
enabled: false  # Sentinel Solution best practice — customer enables per-rule after reviewing query + threshold
requiredDataConnectors:
  - connectorId: XdrLogRaiderInternal
    dataTypes:
      - MDE_Heartbeat_CL
queryFrequency: 15m
queryPeriod: 2h
triggerOperator: gt
triggerThreshold: 0
tactics:
  - PrivilegeEscalation
  - Persistence
relevantTechniques:
  - T1078
query: |
  MDE_Drift_P2Governance(7d, 1d)
  | where StreamName == "MDE_UnifiedRbacRoles_CL"
  | where ChangeType == "Added"
  | where FieldName == "principalId" or FieldName == "principalName"
entityMappings: []
eventGroupingSettings:
  aggregationKind: SingleAlert
suppressionEnabled: false
suppressionDuration: PT5H
