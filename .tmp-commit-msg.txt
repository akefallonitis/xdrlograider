release(v0.1.0): GA — pure Defender connector

Microsoft Sentinel custom data connector for Microsoft Defender XDR
portal-only telemetry. Clean revamp from scratch — no legacy / no
backward-compat. Single consolidated commit replacing all prior
v0.1.0-beta and intermediate iter-* work.

Streams + ingest topology
- 59 streams (46 baseline + 13 Tier A nodoc-catalog additions, all live-captured)
- 11 consolidated workspace tables: 10 Defender_<Category>_CL per nodoc 10-category taxonomy + 1 XdrConnectorHealth_CL ops table
- 7 DCRs sharing 1 DCE (4×10 + 5(7) + 6(7) + 7(6) = 60 streamDecls; respects Azure 10-flow cap)
- DCR transformKql 'source | extend SourceName=...' injection pattern
- RawJson preserved on every row for forensics

Function App + modules
- 7 PowerShell modules: L1 Common.{Auth,Manifest,Telemetry} + L1 Sentinel.Ingest + L2 Defender.{Auth,Client} + L4 Connector.Orchestrator
- 8 Function App functions: Connector-Heartbeat + 5 cadence-tier timers (ActionCenter 10min / XspmGraph 1h / Configuration 6h / Inventory 1d / Maintenance 7d) + Xdr-PollOrchestrator (Durable) + Xdr-PollStream (Activity)
- Manifest-driven dispatch — 1 cmdlet for all 59 streams
- 429 backoff + Retry-After + jitter + 3-retry max
- KV TTL cache (60min default) for credential reuse
- DLQ + checkpoint Storage Tables on shared SA, SAMI-accessed
- AppInsights NATIVE telemetry (Requests/Dependencies/Exceptions/Traces/Events/Metrics)

Sentinel content
- 4 cadence-tier KQL drift parsers using mv-apply set_union
- 8 workbooks (incl. ConnectorHealth with 9 panels)
- 20 analytic rules: 14 detection + 6 XdrOps incl. RowVolumeSpike cost-budget gate — all ship enabled:false
- 9 hunting queries
- 390 sample queries (5 per active stream operator anchor)
- Solution Gallery package: dataTypes=11, IsConnectedQuery, MITRE-validated tactics

ARM + RBAC
- Hand-authored mainTemplate.json (ARM-only deployment, source of truth)
- Deploy URL pins ARM to tag; FA package floats /releases/latest/download/
- 9 role assignments: KV Secrets User + Storage Table Data Contributor + 7 Monitoring Metrics Publisher per DCR
- KV: enableSoftDelete=true, RBAC, 7d retention, purge-protection
- Storage: TLS 1.2 minimum, allowSharedKeyAccess for Y1 AzureWebJobsStorage; SAMI for connector logic

Auth + security
- Auth: Credentials+TOTP + Software Passkey + DirectCookies (test only); CA-compatible
- Service account is the only security boundary
- XdrOps-ServiceAccountAnomalousSignIn analytic rule monitors
- No CVE/SAST scanning; SECURITY.md handles vulnerability disclosure

Testing + CI
- Pester 5.7.1 / 1357+ unit tests / 0 failures / 42 conditional skips
- Coverage 72.51% (>50% v0.1.0 GA gate, >60% production minimum)
- WiringAudit: 59/59 streams × 12 edges intact
- 9-job CI: secret-scan + lint + unit-tests + static-validate + deploy-whatif + auto-recompile-gate + auto-rezip-gate + coverage-gate + summary
- ARM-TTK + Validate-ArmJson + Validate-Manifest gated
- 6 cosign-signed + SBOM-attested release artifacts (function-app.zip, mainTemplate.json, createUiDefinition.json, sentinelContent.json, solution.zip, sbom.spdx.json)

Operator tools
- Smoke-Deploy.ps1 — one-shot post-deploy verdict
- Post-DeploymentVerification.ps1 — P1-P14 probes (XdrConnectorHealth_CL based)
- Test-ConnectorHealth.ps1 — daily check
- Initialize-XdrLogRaiderAuth.ps1 — KV secret seeding
- Cleanup-PriorDeployment.ps1 — Content Hub uninstall + workspace tables/savedSearches/workbooks/rules/connectors via Az REST
- Pre-Commit-Check.ps1 — local CI mirror
